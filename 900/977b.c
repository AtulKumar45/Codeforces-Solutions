#include<stdio.h>
#include<string.h>

// void checkString(char str)
// {
//     int count = 0;
//     for(int i = 0)
// }

int main(){
    int n;
    scanf("%d", &n);
    char str[n];

    fgets(str,sizeof(str),stdin);
    
    //checkString(str);
    int i = 0;
    while(str[i] != '\0')
    {
        if(str)
    }
    return 0;
}